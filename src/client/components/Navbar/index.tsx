import React from 'react';
import { Flex } from 'rebass/styled-components';

// @ts-ignore as is valid prop
const Navbar = () => <Flex as="header" bg="navbar" mb={3} flexGrow={1} flexBasis={20} />;

export default Navbar;
