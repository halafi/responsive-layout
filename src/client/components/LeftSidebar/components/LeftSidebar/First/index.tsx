import React from 'react';
import { Box } from 'rebass/styled-components';

const First = () => (
  <Box
    flexGrow={1}
    mr={[2, null, null, null, 0]}
    mb={[null, null, null, null, 3]}
    bg="leftSidebar0"
  />
);

export default First;
